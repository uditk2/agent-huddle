# Copy to ops/issue_watcher.env and customize.

# Optional explicit repo (owner/name). If empty, auto-detected from git remote.
# GITHUB_REPO=uditk2/agent-huddle

# Service label shown in issue title/body.
WATCH_SERVICE_NAME="nse-insights.service"

# Log fetch command executed every run.
# Available vars: $SINCE_ISO, $NOW_ISO
LOG_FETCH_CMD='journalctl -u nse-insights.service --since "$SINCE_ISO" --no-pager -o short-iso'

# Optional LLM dedupe (OpenAI-compatible API). Model defaults to "sqen".
DEDUPE_MODEL="sqen"
# DEDUPE_API_URL="https://openrouter.ai/api/v1/chat/completions"
# DEDUPE_API_KEY="<token>"
